# 隐私政策 — BookStack

> 适用审核项：C3（隐私政策有无）、C4（完整性）、C5（可访问）、C6（数据收集一致性）、C7（用户数据权利）、C8（第三方 API 说明）

**生效日期**：2026-09-18
**适用版本**：1.0.0

## 一、数据收集与处理

本应用为自托管知识库服务，数据默认保存在本机。
- 页面内容存储于本机挂载目录与内置数据库
- 默认不向任何第三方发送数据
- 如管理员启用邮件通知、OIDC/SAML/LDAP 单点登录，数据将发送至所配置的服务商，该行为由管理员自行配置并负责
- 卸载时若勾选「删除数据」，上述目录将被清除

## 二、数据保存期限

- 运行数据保存在本机挂载目录中，**不设自动过期**，由用户自行管理
- 用户删除对应数据或卸载并选择「删除数据」后即被清除
- 开发者不持有、不接触任何用户数据

## 三、第三方共享

本应用由用户自行部署在本地设备上，开发者不接收任何数据。如上述存在外部服务调用，均由用户自行配置，数据流向以用户所配置的服务商条款为准。

## 四、数据安全措施

- 应用以专用非 root 用户运行
- systemd 启用 `NoNewPrivileges`、`ProtectSystem=strict`、`ProtectHome`、`PrivateTmp` 等隔离措施
- 数据仅保存在本机文件系统，不经由开发者服务器中转

## 五、用户数据权利

用户可随时通过以下方式查阅、更正、删除自己的数据：

1. **查阅 / 更正**：通过应用自身的 WebUI 操作
2. **删除**：删除本机数据目录，或在卸载应用时选择「同时删除数据」
3. **完全移除**：执行 `dpkg --purge`（Deb 应用）或删除 `/Volume*/DockerAppData/<appid>/`（Docker 应用）

## 六、联系方式

如对隐私政策有疑问，请通过上游项目仓库提交 issue：https://github.com/BookStackApp/BookStack
